# Build
/target/
**/*.rs.bk

# OS / editor
.DS_Store
**/.DS_Store
*.swp
*~
.idea/
.vscode/
*.code-workspace

# Local env / secrets (never commit)
.env
.env.*
!.env.example

# Tooling / session noise
sessions/
.grok/
*.log

# Occasional local scratch (keep probes under tests/clang-probe/)
/tmp/
.tmp-bottle/
.kh/

# Linux guest/host crash dumps (kh disables RLIMIT_CORE; still never commit)
/core
core
core.[0-9]*
*.core

# Staged guest dylib / release layout (scripts/stage-libsystem.sh)
/dist/

# Local guest/docker scratch dirs
.tmp-*/
